# Tasks

- [/] Component 1: Log Visit (`ClinicalEntryScreen`)
  - [ ] Initialize `_postOpReferredToController` and wire up `dispose()`, `_resetForm()`.
  - [ ] Modify `_onCompleteVisit()` to include `'post_op_referred_to': _postOpReferredToController.text.trim()`.
  - [ ] Remove `_buildVitalsSection()` from `build()` layout.
  - [ ] Remove `Prescriptions` `NeuTextField` from `_buildClinicalNotesSection()` and replace it with `Post Op Referred To (Radiation Oncology) :` field.
- [ ] Component 2: External Doctor Directory & Providers
  - [ ] Update `ExternalDoctor` model class in `external_doctors_provider.dart` to support `areaDistrict` and `meetDrType`.
  - [ ] Modify `ExternalDoctorsNotifier.add()` and `updateDoctor()` to accept and save `areaDistrict` and `meetDrType`.
  - [ ] Add `District` and `Type Of Doctor` dropdown fields in `_AddExternalDoctorSheetState`'s build method inside `network_directory_screen.dart`.
- [ ] Component 3: New External Visit Form
  - [ ] Add `noOfPatientsReceived` and `workPending` fields to `AgentOutsideVisit` model.
  - [ ] Update `agent_outside_visit_provider.dart` to support `no_of_patients_received` and `work_pending` in `createVisit` and `updateVisit`.
  - [ ] In `agent_outside_visit_form.dart`:
    - [ ] Add `_noOfPatientsCtrl` and `_workPendingCtrl` text controllers.
    - [ ] Add `District Dropdown` and `Type of Doctor` dropdowns in `_ExternalDoctorPickerSheetState._buildAddForm()`.
    - [ ] Simplify `AgentOutsideVisitForm`'s Doctor Card: remove doctor name, specialization, phone, district, and type of doctor inputs, leaving only Hospital/Clinic and How Many Times Visited. Autocomplete or default the mandatory `extDoctorName` field to the hospital/clinic name or a generic "External Doctor".
    - [ ] Simplify `AgentOutsideVisitForm`'s Outcome Card: remove prescriptions, diagnosis, and chief complaint. Add `No of Patients Received` and `Work Pending` inputs.
- [ ] Component 4: Testing & Verification
  - [ ] Run `flutter analyze` to verify code correctness and ensure it compiles successfully.
